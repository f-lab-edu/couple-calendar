# Public — exposed to the browser bundle
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080

# Server-only — read in RSC / route handlers / middleware
API_BASE_URL=http://localhost:8080
AUTH_COOKIE_NAME=cc-auth
